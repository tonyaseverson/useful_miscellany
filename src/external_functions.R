# Function multiplies two input values
multiply_values <- function(x, y) {
  r = x*y
  return(r)
}

# Function repeats string s, n times
ridiculous_repetition <- function(s,n) {
  v = rep(s, n)
  r = paste(v, sep = ' ')
  return(r)
}

myHW <- "Hello, world!"


